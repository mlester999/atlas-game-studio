import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Client-side planning studio: all project data lives in the browser
  // (IndexedDB / localStorage). No backend, no external services.
};

export default nextConfig;
